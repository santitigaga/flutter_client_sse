name: client_sse
description: Dart package to help consume SSE API. It consumes server sent events by returning parsed model of the event, id, and the data
version: 1.0.0-beta
homepage: https://github.com/santitigaga/flutter_client_sse

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  logging: ^1.2.0

dev_dependencies:
  test: ^1.21.4
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

flutter:

